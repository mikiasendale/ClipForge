# ClipForge secrets — copy to .env (gitignored). Never commit .env.
OPENROUTER_API_KEY=

# Optional: absolute path to ffmpeg if not on PATH and not in ./tools
# FFMPEG_PATH=/absolute/path/to/ffmpeg
# FFPROBE_PATH=/absolute/path/to/ffprobe
