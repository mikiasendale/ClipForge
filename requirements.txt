# ClipForge — pinned dependencies (Python 3.12)
# Install: .venv/bin/pip install -r requirements.txt

# Video / audio acquisition & analysis
yt-dlp==2026.8.19
faster-whisper==1.2.1
scenedetect==0.7.1
opencv-python-headless==4.12.0.88  # 4.x line: OpenCV 5.x dropped the Haar CascadeClassifier API
numpy==2.2.6   # opencv-python-headless 4.12 wheels are built against numpy<2.3 ABI
soundfile==0.14.0

# Web scraping / channel enrichment (StealthyFetcher browser is fetched via `scrapling install`)
scrapling==0.4.15

# CapCut / JianYing draft export (optional second output mode — feature B).
# Reverse-engineered format: a CapCut auto-update can break it. mp4 mode is
# independent of this package; if it's missing the UI disables CapCut mode.
pyJianYingDraft==0.3.0

# Web app
fastapi==0.141.1
uvicorn[standard]==0.53.0
jinja2==3.1.6
python-multipart==0.0.32

# HTTP + config
requests==2.34.2
PyYAML==6.0.3

# Proactive scheduling (P0) — APScheduler drives the daily job + pre-staging
APScheduler==3.11.3

# Tests (optional, for developers)
pytest==9.1.1
httpx==0.28.1
